实现轮转法、短进程优先调度算法，来分别对两个进程队列RQ1、RQ2进行进程调度，实现多级队列调度算法（RQ1 > RQ2）。
时间片轮转调度算法：用于分时系统中的进程调度。每次调度时，总是选择就绪队列的队首进程，让其在CPU上运行一个系统预先设置好的时间片。一个时间片内没有完成运行的进程，返回到绪队列末尾重新排队，等待下一次调度。
短进程优先调度算法：SJF(shortest job first)是以进程的运行时间长度作为优先级，进程运行时间越短，优先级越高。
多级队列调度算法：将就绪队列分成多个单独队列。根据进程属性，如内存大小、进程优先级、进程类型等，一个进程永久分到一个队列，每个队列有自己的调度算法。
