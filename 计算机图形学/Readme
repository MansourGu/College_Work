第一次：
1、基本图形生成算法（直线、圆、椭圆）必做。
2、改进有效边表算法必做。
3、种子填充选做。
第二次：
1、自己写矩阵完成二维图形变换。必做
2、编码裁剪算法。必做
3、sw双边裁剪算法。选做
第三次：
1、完成日地月模型。必做
2、完成三视图变换。必做
3、单位立方体投影变换（采用多边形表面模型生成立方体，不要用 glutWireCube直接生成立方体）选做
第四次：
开放式作业，自学OpenGL的函数，最好生成可以动起来的三维物体，比如可以加上光照、纹理等。
